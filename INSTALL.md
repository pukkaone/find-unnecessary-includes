# Prerequisites

* Git
* CMake 2.8 or better


# Check out source

After cloning the project Git repository, run

    git submodule init
    git submodule update


# Build

Run the commands:

    mkdir build
    cd build
    cmake ..
